diff --git a/dist/cog-layer.d.ts b/dist/cog-layer.d.ts
index 167756a51946449f1be2f864f61a5edd36e5bf9e..9b16cc3ce84dce32539fd5d9e1fc12de9e39b2ad 100644
--- a/dist/cog-layer.d.ts
+++ b/dist/cog-layer.d.ts
@@ -85,6 +85,13 @@ export type COGLayerProps<DataT extends MinimalTileData = DefaultDataT> = Omit<R
             north: number;
         };
     }) => void;
+    /**
+     * Called when CRS parsing or projection setup fails and the layer cannot
+     * be rendered.  Provides a hook to surface the error to the user without
+     * relying on the browser's unhandledrejection event (which may be
+     * intercepted by error-monitoring tools before the app sees it).
+     */
+    onError?: (err: unknown) => void;
     /** A user-provided AbortSignal to cancel loading.
      *
      * This can be useful in combination with the MosaicLayer, so that when a
diff --git a/dist/cog-layer.js b/dist/cog-layer.js
index 3cd2714260086e7c8978d459af6716b8469103b5..c1d79c6281e1fd4ff3dc46b8f220020086071eab 100644
--- a/dist/cog-layer.js
+++ b/dist/cog-layer.js
@@ -20,6 +20,7 @@ export class COGLayer extends RasterTileLayer {
         ...RasterTileLayer.defaultProps,
         epsgResolver,
         concurrencyLimiter: DEFAULT_CONCURRENCY_LIMITER,
+        onError: null,
     };
     initializeState() {
         this.setState({ abortController: new AbortController() });
@@ -36,7 +37,11 @@ export class COGLayer extends RasterTileLayer {
             // Clear stale state so renderLayers returns null until the new GeoTIFF is
             // ready
             this.clearState();
-            this._parseGeoTIFF();
+            this._parseGeoTIFF().catch(err => {
+                if (!this.state.abortController?.signal?.aborted) {
+                    this.props.onError?.(err);
+                }
+            });
         }
     }
     clearState() {
@@ -64,9 +69,25 @@ export class COGLayer extends RasterTileLayer {
             throw err;
         }
         const crs = geotiff.crs;
-        const sourceProjection = typeof crs === "number"
-            ? await this.props.epsgResolver(crs)
-            : parseWkt(crs);
+        let sourceProjection;
+        if (typeof crs === "number") {
+            sourceProjection = await this.props.epsgResolver(crs);
+        } else {
+            try {
+                sourceProjection = parseWkt(crs);
+            } catch (wktErr) {
+                // wkt-parser doesn't support every projection method.  Many COGs
+                // embed an EPSG AUTHORITY tag in their WKT — extract it and resolve
+                // via epsg.io as a fallback so the file still renders.
+                const matches = [...crs.matchAll(/AUTHORITY\["EPSG","(\d+)"\]/gi)];
+                const epsgCode = matches.length > 0 ? Number(matches[matches.length - 1][1]) : NaN;
+                if (!isNaN(epsgCode)) {
+                    sourceProjection = await this.props.epsgResolver(epsgCode);
+                } else {
+                    throw wktErr;
+                }
+            }
+        }
         if (signal?.aborted) {
             return;
         }
diff --git a/dist/geotiff/geotiff.js b/dist/geotiff/geotiff.js
index b07748d892c3f174650047cbae6285ab5fa94efa..471b81549f455a4c5f33c0685d5ba5c9f2261934 100644
--- a/dist/geotiff/geotiff.js
+++ b/dist/geotiff/geotiff.js
@@ -54,24 +54,43 @@ export async function fetchGeoTIFF(input, options = {}) {
 }
 /**
  * Calculate the WGS84 bounding box of a GeoTIFF image
+ *
+ * Samples many points along each edge of the native-CRS bounding box rather
+ * than only the 4 corners.  For bounded-domain projections (e.g. World
+ * Mollweide / ESRI:54009) the rectangular bounding box extends outside the
+ * valid domain, so corner projections return [NaN, NaN].  Dense edge sampling
+ * finds the valid points where the CRS boundary intersects the box edges and
+ * produces a correct WGS84 envelope.
  */
 export function getGeographicBounds(geotiff, converter) {
     const [minX, minY, maxX, maxY] = geotiff.bbox;
-    // Reproject all four corners to handle rotation/skew
-    const corners = [
-        converter.forward([minX, minY]), // bottom-left
-        converter.forward([maxX, minY]), // bottom-right
-        converter.forward([maxX, maxY]), // top-right
-        converter.forward([minX, maxY]), // top-left
-    ];
-    // Find the bounding box that encompasses all reprojected corners
-    const lons = corners.map((c) => c[0]);
-    const lats = corners.map((c) => c[1]);
-    const west = Math.min(...lons);
-    const south = Math.min(...lats);
-    const east = Math.max(...lons);
-    const north = Math.max(...lats);
-    // Return bounds in MapLibre format: [[west, south], [east, north]]
-    return { west, south, east, north };
+    const SAMPLES = 20;
+    const validLons = [];
+    const validLats = [];
+    const tryProject = (x, y) => {
+        const [lon, lat] = converter.forward([x, y]);
+        if (Number.isFinite(lon) && Number.isFinite(lat)) {
+            validLons.push(lon);
+            validLats.push(lat);
+        }
+    };
+    for (let i = 0; i <= SAMPLES; i++) {
+        const t = i / SAMPLES;
+        const mx = minX + t * (maxX - minX);
+        const my = minY + t * (maxY - minY);
+        tryProject(mx, minY); // bottom edge
+        tryProject(mx, maxY); // top edge
+        tryProject(minX, my); // left edge
+        tryProject(maxX, my); // right edge
+    }
+    if (validLons.length === 0) {
+        return { west: NaN, south: NaN, east: NaN, north: NaN };
+    }
+    return {
+        west: Math.min(...validLons),
+        south: Math.min(...validLats),
+        east: Math.max(...validLons),
+        north: Math.max(...validLats),
+    };
 }
 //# sourceMappingURL=geotiff.js.map
